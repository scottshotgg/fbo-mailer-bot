DATE=`date +%d-%m-%Y:%H:%M:%S`
echo $DATE

if [ "$1" = "server" ]; then
	cd /home/fborobo/fbo-mailer-bot/
	#DEBUG=nightmare node spider.js 2>&1 | tee logs/log+$DATE
	DEBUG=nightmare node spider.js
else
	cd /home/scottshotgg/Development/fbo-mailer-bot/
	#DEBUG=nightmare node spider.js 2>&1 | tee logs/log+$DATE
	DEBUG=nightmare node spider.js
fi

wait

if [ "$1" = "server" ]; then
	cp index.html /var/www/html/index.html
else
	chown nobody:nogroup /var/ftp/fbo-mailer-bot
	chown nobody:nogroup /var/ftp/fbo-mailer-bot/*
fi